import React, {useState} from "react";
import {Container, Typography} from "@mui/material";
import "./Main.css"

function Main() {
    return (
        <Container>
            <Container className={"container-offset"}>
            <Typography className={"rolling-text"}> Call me Placeholder </Typography>
            </Container>
            </Container>
    );
}

export default Main;